buildscript {
    repositories { jcenter() }
	dependencies { classpath files('../../ext/gradle-spl-plugin-1.0.0-SNAPSHOT.jar') }
}

apply plugin: 'streamsx.build'

dependencies {
	toolkit files('../../com.ibm.streamsx.weather')
}

streamsBuildConfig {
  scCompile {
    namespace = 'com.ibm.streamsx.weather.sample'
    composite = 'CountryAreaSample'
    outputDirectory = 'output/' + composite
  }
  scCompile {
    namespace = 'com.ibm.streamsx.weather.sample'
    composite = 'CountryGeocodeSample'
    outputDirectory = 'output/' + composite
  }
  scCompile {
    namespace = 'com.ibm.streamsx.weather.sample'
    composite = 'CountrySample'
    outputDirectory = 'output/' + composite
  }
  scCompile {
    namespace = 'com.ibm.streamsx.weather.sample'
    composite = 'CountryStateSample'
    outputDirectory = 'output/' + composite
  }
  scCompile {
    namespace = 'com.ibm.streamsx.weather.sample'
    composite = 'DetailsSample'
    outputDirectory = 'output/' + composite
  }      
}
